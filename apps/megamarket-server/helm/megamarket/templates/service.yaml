apiVersion: v1
kind: Service
metadata:
  name: {{ include "megamarket.fullname" . }}
  labels:
    {{- include "megamarket.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "megamarket.selectorLabels" . | nindent 4 }}
