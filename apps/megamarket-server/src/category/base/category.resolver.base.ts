/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { Category } from "./Category";
import { CategoryCountArgs } from "./CategoryCountArgs";
import { CategoryFindManyArgs } from "./CategoryFindManyArgs";
import { CategoryFindUniqueArgs } from "./CategoryFindUniqueArgs";
import { CreateCategoryArgs } from "./CreateCategoryArgs";
import { UpdateCategoryArgs } from "./UpdateCategoryArgs";
import { DeleteCategoryArgs } from "./DeleteCategoryArgs";
import { CategoryItemFindManyArgs } from "../../categoryItem/base/CategoryItemFindManyArgs";
import { CategoryItem } from "../../categoryItem/base/CategoryItem";
import { CategoryService } from "../category.service";
@graphql.Resolver(() => Category)
export class CategoryResolverBase {
  constructor(protected readonly service: CategoryService) {}

  async _categoriesMeta(
    @graphql.Args() args: CategoryCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [Category])
  async categories(
    @graphql.Args() args: CategoryFindManyArgs
  ): Promise<Category[]> {
    return this.service.categories(args);
  }

  @graphql.Query(() => Category, { nullable: true })
  async category(
    @graphql.Args() args: CategoryFindUniqueArgs
  ): Promise<Category | null> {
    const result = await this.service.category(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => Category)
  async createCategory(
    @graphql.Args() args: CreateCategoryArgs
  ): Promise<Category> {
    return await this.service.createCategory({
      ...args,
      data: {
        ...args.data,

        cat_Id: args.data.cat_Id
          ? {
              connect: args.data.cat_Id,
            }
          : undefined,

        parentId: args.data.parentId
          ? {
              connect: args.data.parentId,
            }
          : undefined,
      },
    });
  }

  @graphql.Mutation(() => Category)
  async updateCategory(
    @graphql.Args() args: UpdateCategoryArgs
  ): Promise<Category | null> {
    try {
      return await this.service.updateCategory({
        ...args,
        data: {
          ...args.data,

          cat_Id: args.data.cat_Id
            ? {
                connect: args.data.cat_Id,
              }
            : undefined,

          parentId: args.data.parentId
            ? {
                connect: args.data.parentId,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => Category)
  async deleteCategory(
    @graphql.Args() args: DeleteCategoryArgs
  ): Promise<Category | null> {
    try {
      return await this.service.deleteCategory(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => [CategoryItem], { name: "categoryItems" })
  async findCategoryItems(
    @graphql.Parent() parent: Category,
    @graphql.Args() args: CategoryItemFindManyArgs
  ): Promise<CategoryItem[]> {
    const results = await this.service.findCategoryItems(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @graphql.ResolveField(() => Category, {
    nullable: true,
    name: "catId",
  })
  async getCatId(@graphql.Parent() parent: Category): Promise<Category | null> {
    const result = await this.service.getCatId(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @graphql.ResolveField(() => Category, {
    nullable: true,
    name: "parentId",
  })
  async getParentId(
    @graphql.Parent() parent: Category
  ): Promise<Category | null> {
    const result = await this.service.getParentId(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
